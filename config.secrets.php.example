<?php
/**
 * Copy this file to config.secrets.php and fill in real values.
 * config.secrets.php is git-ignored — never commit it.
 *
 * Only the keys you set here override config.php; everything else
 * falls back to the values in config.php / system_settings.
 */
return [
    // 'APP_SECRET' => 'a-64-char-random-string',
    // 'DB_PASS'    => 'the-mysql-password',
    // 'TELEGRAM_BOT_TOKEN' => '123456:ABC...',
];
