# Connectix App — CHANGELOG

## 3.3.7
- رفع مشکل «بدون سرور»: افزایش مهلت دریافت لیست سرورها (۲۰ ثانیه) + ذخیره‌ساز ۹۰ ثانیه‌ای سمت پنل برای پاسخ‌های سریع
- لاگ کامل مسیر دریافت سرورها (آپی / ساب‌لینک / دلیل رد شدن) برای ارسال در گزارش فنی
- راهنمای حل در وضعیت «هیچ کانکشنی یافت نشد» (تازه‌سازی یا ورود مجدد)

## 3.3.6
- رفع مشکل نصب و اجرای نسخهٔ 3.3.5 (نسخهٔ 3.3.5 نصب‌نشود)
- تشخیص کامل کرش‌های لایه اندروید: خطاها قبل از شروع Flutter هم ثبت، در بارگذاری بعدی نمایش و ارسال به پشتیبانی می‌شوند

## 3.3.5 — ❌ معیوب، نصب نکنید
- تشخیص کامل کرش‌های لایه اندروید: حتی اگر خطا قبل از شروع Flutter رخ دهد، ثبت و در بارگذاری بعدی نمایش داده و به پشتیبانی ارسال می‌شود

## 3.3.4
- ویندوز: حالت «VPN کامل (TUN)» — تمام برنامه‌ها (حتی UDP/بازی‌ها) از تونل عبور می‌کنند + انتخاب‌گر حالت اتصال (پروکسی سیستمی / VPN کامل)
- ویندوز: پنجره به‌اندازه (نه بیشتر از ۵۲×۸۸۰) و مرتب
- فونت فارسی وزیرمتن در کل برنامه (آندروید و ویندوز) — خوانایی و زیبایی بهتر
- تم مدرن‌تر (Material 3): دکمه‌ها، کارت‌ها، دیالوگ‌ها و فیلدها یکدست‌تر و زنده‌تر

## 3.3.3
- رفع کرش لحظه‌ای هنگام باز شدن برنامه روی برخی دستگاه‌ها (بازگشت پلاگین تشخیص شبکه به نسخه پایدار + غیرفعال‌سازی ماژول‌های استارتاپ اضافه اندروید)
- صفحه گزارش خطا: اگر برنامه هرگز با خطایی مواجه شود، به‌جای بسته شدن بی‌صدا، صفحه‌ای با جزئیات خطا و دکمه «ارسال گزارش فنی» نمایش داده می‌شود
- تشخیص خودکار کرش‌های ناگهانی: اگر برنامه قبلی بدون خروج طبیعی بسته شده باشد، در بارگذاری بعدی یک گزارش فنی خودکار برای پشتیبانی ارسال می‌شود

## 3.3.2
- گزینه «دانلود فقط روی Wi-Fi» برای آپدیت (صرفه‌جویی در اینترنت همراه)
- دکمه «ارسال گزارش فنی» در صفحه خطای دانلود؛ لاگ کامل دستگاه به‌صورت خودکار برای پشتیبانی ارسال می‌شود
- versionCode/نام نسخه اندروید اکنون با نسخه واقعی هم‌راستا است (۱.۰.۱۱)

## 3.3.1
- دانلود مقاوم و تکه‌ای آپدیت (هر ۱ مگابایت جدا)؛ اگر اتصال وسط دانلود قطع شود، از همان نقطه ادامه می‌دهد و از اول شروع نمی‌شود
- رفع خطای «قطع دانلود و خطا در ارتباط» هنگام بروزرسانی روی شبکه‌های ناپایدار
- تلاش خودکار تکراری برای هر تکه دانلودشده بدون دخالت کاربر

## 3.3.0
- نمایش دائمی نسخه اپلیکیشن در برنامه (صفحه ورود و داشبورد)
- نمایش لوگو، نام برند و رنگ اختصاصی نماینده در اپلیکیشن (White-Label)
- نمایش آیدی پشتیبانی اختصاصی نماینده در بخش پشتیبانی و پاورقی
- نمایش اطلاعیه مدیریتی/نماینده در بالای داشبورد
- لینک جایگزین خودکار هنگام قطع بودن لینک اصلی دانلود آپدیت

## 3.2.0
- بهبود به‌روزرسانی درون‌برنامه‌ای: تلاش خودکار با لینک جایگزین گیت‌هاب
- نصب آپدیت مستقیم روی نسخه فعلی (بدون حذف و نصب مجدد)
